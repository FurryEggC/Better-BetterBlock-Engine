///scrButtonCheck(buttonArray)
///checks whether a button is currently being pressed
///argument0 - array containing the keyboard button in index 0

var buttonIn = argument0;

return (keyboard_check(buttonIn));

